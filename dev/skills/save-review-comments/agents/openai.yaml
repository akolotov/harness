interface:
  display_name: "Save Review Comments"
  short_description: "Persist actionable review comments"
  default_prompt: "Use $save-review-comments to save the surviving actionable review comments from this session."
policy:
  allow_implicit_invocation: false
